# Saffron Afterimage — Project 026

A React/Vite/GSAP cinematic Indian wedding editorial built around saffron dusk, ritual detail and photographic memory.

## Factory gates

- `npm run build` is the source/pre-deploy gate.
- Expected Vercel project: `tasksk026`.
- Canonical production URL: `https://tasksk026.vercel.app`.
- Production approval requires exact-current-commit Vercel SUCCESS plus the real four-viewport QA path (390×844, 768×1024, 1366×768, 1920×1080) and reduced-motion check.
- A workflow run that only reaches “Waiting for Vercel Git integration” is explicitly non-approving.
